/* import Context from "./Context";
import { useContext } from "react";

function Feed() {
    const { list } = useContext(Context);
    console.log(list)
    return ( 
        <div>
            {""}
            Feed
        </div>
     );
}

export default Feed; */